using System;

namespace Homework04
{
    class Program
    {
        static void Main(string[] args)
        {
            #region Task0
            //Create a new method called Substrings
            //In that method enter the following string :
            //"Hello from SEDC Codecademy v7.0"
            //Ask the user to enter a number n.
            //Print the first n characters from that string and print the length of the new string.
            //Make sure that the user can only enter a number and the number should be from 1 to the number of 
            //characters in the string(cant enter an integer larger than the length of the string)

            //string message1 = Substrings();
            //Console.WriteLine("Enter a number");
            //int n;
            //int.TryParse(Console.ReadLine(), out n);
            //if (n > message1.Length)
            //{
            //    Console.WriteLine($"Please enter number lower than {message1.Length} ");
            //}
            //var nCharacter = message1.Substring(n);
            //Console.WriteLine(nCharacter, nCharacter.Length);

            //static void Substrings()
            //{
            //    string message = "Hello from SEDC Codecademy v7.0";

            
        }
        #endregion
        #region Task1
        /*Create a function to say whether a string is symmetric(a palindrome).*/
        //static void Main(string[] args)
        //{

        //}

        //    public static bool IsPalindrome(string myString)
        //    {
               
        //        int length = myString.Length;
        //        for (int i = 0; i < length / 2; i++)
        //        {
        //            if (myString[i] != myString[length - i - 1])
        //                return false;
        //        }
        //        return true;
        //    }
            
                
            
            #endregion
        

    }
}

